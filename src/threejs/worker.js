export default () => {

  
  onmessage = event => {
    console.log(event.data);

    // for(let i = 0; i < this.particles.length; i++ ){
    //   this.particles[i].update()
    // }
    // postMessage({ foo: 'foo' })
  }
  
}